# Domain docs

Use the single-context layout for this service:

- Read `/CONTEXT.md` before exploring production behavior.
- Read relevant decisions under `/docs/adr/` before changing the same area.
- Use the vocabulary defined in `CONTEXT.md` in tests, commits, and operational handoffs.
- Surface conflicts with an existing ADR instead of silently overriding it.
