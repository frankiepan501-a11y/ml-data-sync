# 官方账单上传入口：独立测试阶段

范围：固定2026-08，三个已批准店铺，财务助手仅向Frankie发送链接卡。未启用正式核销或9月18日提醒。上传不代表运营或财务确认。

实现：billing_upload.py持久保存原件、收据、来源声明与短期凭证摘要；billing_upload_api.py提供上传/下载/清单及单进程解析队列；billing_upload_parse.py在独立子进程中读取，60秒超时终止；intake_web为中文提交表单。测试库位于ML持久卷的ml-intake-test/uploads.sqlite，原件以SQLite BLOB事务落盘、下载前核对SHA256。不触碰正式月结、毛利表、公司汇总、工资或付款。

约束：20MB/文件、30文件及200MB/批次；同店同用途同内容返回旧收据；同名不同内容保留两份。来源分类只是提交人声明，不代表官方可信性。缺件、读取失败、CBT未适配与账期待核实分开展示，ready_for_final_confirmation始终false。

验证：ML离线200项通过，财务服务36项通过。历史BR/MX3官方原件本地隔离读取分别2455/2899行，读取耗时0.91/1.08秒；保存和下载字节完全一致，重复上传收据不增。BR文件含多个费用月份，不能整份归入8月。以目前样本估算30份约33秒纯解析，加队列每份约2秒调度间隔仍由后台完成；最坏每份60秒、整批约31分钟，不依赖HTTP同步等待。大文件上限场景仍须后续容量观察。

双轴审查：规格与规范各复查通过。补齐来源分类，压缩方法99等损坏Excel转明确400错误；不会让同事反复重传坏文件。SQLite上下文只提交事务不自动关闭连接，显式close确保Windows测试和真实进程释放文件。

发布验收仍待：云端真实原件上传、重启后回读、Frankie客户端打开卡并上传、8月原状态及公司汇总不变。未取得这些证据前第2项不得标完成。

回滚：ML恢复9f43dff，财务服务恢复460adcd。保留独立测试库及收件证据，不能删除正式财务数据。只恢复代码，不修改环境变量或权限。
