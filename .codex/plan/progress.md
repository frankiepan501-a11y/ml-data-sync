# 进度记录

## 2026-08-11

- 财务确认 2026-07 标准表没有问题，授权固化到未来月报生成器。
- 已定位生产仓库 `ml-data-sync`；当前生产服务有月度汇总和月结确认，但没有 47 列标准飞书表生成器。
- 已确认一次性脚本生成的标准表包含 47 列主表、数据源、检查三个 Sheet。
- 已确认 ERP 映射业务口径：成本按 ERP SKU；展示名称/分类来自 ERP 产品资料；异常必须拦截整份报告。
- 已读取项目 AGENTS、CONTEXT、领域文档、财务 fail-closed ADR、飞书 App 台账及相关 skill。
- 已读取财务确认回调和飞书 Sheet API 实现，并按失败场景先补测试。
- 新增 `app/unified_report.py`：47 列公式、三个 Sheet 数据、ERP 映射、wiki 模板复制、完成标记和写后回读。
- 财务确认改为“先生成月报，后写财务终稿”；生成失败写异常并清空按钮去重键，修复资料后可重试。
- 新增只读预览/幂等回放接口 `/report/ml-unified-monthly`；直接提交只允许已财务确认月份。
- 全量 70 个测试通过，`py_compile` 和 `git diff --check` 通过。
- 使用 2026-07 真实飞书数据只读预演通过：57 行、44 唯一 SKU、3 店、47 主列、49 数据源列、产品字段空值 0；ERP 映射 35 个来自维护表、9 个来自成本台；佣金最大舍入差 0.034328 RMB，毛利重算差 0。
- 代码复核加固：三张 Sheet 全量回读后才写 COMPLETE；ERP 展示值和全部公式逐格校验；SQLite 按月份跨进程防重；广告失败/退回可取消生成；补数或映射变化时原表内刷新。
- 最终复审加固：SQLite 永久记录已完成的飞书动作 key，旧卡延迟重放不能倒退终稿；终态写入暂时失败会把动作标为可重试，同一张财务卡可再次确认。
- 并发复审加固：所有已领取动作都有统一异常回收；SQLite 按月份记录动作先后，只有最新动作能写最终状态，“处理中”与“已完成”分别反馈。
- 重算复审加固：成本计算与月结状态提交拆开；旧重算可更新成本明细，但只有仍为最新动作时才能改月结状态或发结果卡。状态动作不再用无序的飞书“最后 key”裁决并发，SQLite 顺序是唯一依据。
- 最后并发加固：成本逐行写入期间用 SQLite 持久工作标记阻止运营/财务确认；迟到旧卡先校验 message_id，无效动作不占用新的动作顺序。
- 广告失败与财务终稿的两种并发先后均已覆盖：失败标记与报表取消在同一 SQLite 事务中，不会留下错误终稿。
- 已补 README、ADR 0002 和环境变量样例；生产可复用现有聪哥 2 号凭据，不需要改环境变量。下一步：待 Frankie 确认生产写入后推送并 redeploy。
