# 进度记录

## 2026-08-11

- 财务确认 2026-07 标准表没有问题，授权固化到未来月报生成器。
- 已定位生产仓库 `ml-data-sync`；当前生产服务有月度汇总和月结确认，但没有 47 列标准飞书表生成器。
- 已确认一次性脚本生成的标准表包含 47 列主表、数据源、检查三个 Sheet。
- 已确认 ERP 映射业务口径：成本按 ERP SKU；展示名称/分类来自 ERP 产品资料；异常必须拦截整份报告。
- 已读取项目 AGENTS、CONTEXT、领域文档、财务 fail-closed ADR、飞书 App 台账及相关 skill。
- 下一步：读取财务确认回调和飞书 Sheet API 实现，先写失败测试。
- 新增 `app/unified_report.py`：47 列公式、三个 Sheet 数据、ERP 映射、wiki 模板复制、完成标记和写后回读。
- 财务确认改为“先生成月报，后写财务终稿”；生成失败写异常并清空按钮去重键，修复资料后可重试。
- 新增只读预览/幂等回放接口 `/report/ml-unified-monthly`；直接提交只允许已财务确认月份。
- 新增 10 个专项测试；全量 43 个测试通过，`py_compile` 通过。
- 使用 2026-07 真实飞书数据只读预演通过：57 行、44 唯一 SKU、3 店、47 主列、49 数据源列、产品字段空值 0；ERP 映射 35 个来自维护表、9 个来自成本台；佣金最大舍入差 0.034328 RMB，毛利重算差 0。
- 已补 README、CONTEXT、ADR 0002 和环境变量样例；下一步做代码审查、提交、部署和生产只读验证。
